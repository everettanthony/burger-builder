import React from 'react';
import burgerLogo from '../../assets/images/burger-logo.png';

const logo = (props) => (
    <div>
        <img src={burgerLogo} alt="Burger Builder" height="45" width="70" />
    </div>
);

export default logo;
